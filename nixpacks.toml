[phases.setup]
nixPkgs = ["python312", "gcc", "libxml2", "libxslt"]

[phases.install]
cmds = ["pip install --upgrade setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2"
